import heapq

class Solution:
    def findKthLargest(self, nums, k):
        # min-heap of size k
        heap = []
        for num in nums:
            heapq.heappush(heap, num)
            if len(heap) > k:
                heapq.heappop(heap)
        return heap[0]
